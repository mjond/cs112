/*
 * fibonacci array = array[40]
 * preassign array[0] = 0
 * 			 array[1] = 1
 * for i in the array:
 * 		fib[i] = fib[i-1] + fib[i-2]
 */

#include <iostream>
using namespace std;

int main() {
	string name;
	cout << "Enter your name: " << flush;
	cin >> name;
	cout << "Go shower you smell like shit, " << name << flush;
}
